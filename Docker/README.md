# Docker - Application Containerization

This directory contains the Dockerfile used to build the application container image.

## Steps to Build the Image

```bash
docker build -t app-image:latest .

